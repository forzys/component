

import { BrowserRouter } from "react-router-dom";
import Router from "@/routers/index";


export default (props)=>{ 
    return (
        <BrowserRouter>
            <Router />
        </BrowserRouter>
    )
}